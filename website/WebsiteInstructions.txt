# VECTOR ANOMALY — WEBSITE SPECIFICATION SYSTEM BRIEF

This master specification defines the architectural, visual, structural, and behavioral requirements for the **VECTOR ANOMALY Web Terminal Landing Page**. This document serves as a high-fidelity, actionable system prompt optimized for deployment within AI-driven engineering spaces (such as **Cursor, Windsurf, Codex, and Antigravity environments**). It translates game mechanics, telemetry architecture, and brand logic directly into web constructs.

---

Establish the website's visual foundation to mirror the game's identity as a collapsing, hostile vector simulation. The landing page must feel like an operational physics telemetry terminal or diagnostic HUD rather than a generic video game website. The color palette must strictly obey the game's progression cycle: starting completely stark with true `#000000` black backgrounds, pure `#FFFFFF` white sharp vector geometry, and crisp telemetry typography (`JetBrains Mono` or similar system font), then intentionally fracturing into cold cyan, warning amber, and critical red as the user scrolls. Avoid muddy background gradients, generic space fantasy nebulae, or unreadable bloom fog; everything must feel precise, dangerous, intelligent, and completely deterministic, delivering the core market hook within three seconds of loading.

The fold of the page must prioritize immediate cognitive capture using a centered, raw-canvas layout that houses the primary gameplay trailer framed like an active simulation window. The trailer itself must not display abstract neon decoration or cinematic fluff, but rather start immediately with a high-intensity, 3-second hook clip showing a near-death orbit escape or high-speed gravity collapse. Encircling this media window, an active vector-line container should render automated system diagnostics: real-time frame timing, simulated seed codes (`mode:seed:wave`), and flashing alerts reading `LAWS CRACKING: WAVE GENERATOR OFFLINE`. Flanking the video element, prominent, low-latency call-to-action buttons for "Wishlist on Steam" and "Play Demo" must be rendered with razor-sharp geometric borders that subtly pulse with a physics-driven hover velocity effect, rather than generic CSS color fades.

Directly below the fold, implement an interactive, ultra-lightweight HTML5 Canvas or WebGL canvas element that serves as a playable, miniature gravity-slingshot simulator. This sandbox allows visitors to click and drag to launch a vector probe (representing the player control vector) around a central, pulsing black hole mass node (the Accretion Core). The canvas must mirror the game's actual physics principles: momentum inheritance, vector trajectories drawn via dotted `Line2D` styled lines, and localized time dilation pockets that slow down the probe when it enters an explicit zone circle. If the user successfully executes a perfect apex slingshot recovery, the sandbox emits a controlled, low-alpha burst flash utilizing `Settings.flash_alpha()` logic, momentarily rendering glowing resonance glyphs (compression, slipstream, inversion) on-screen to trigger the realization that "you do not shoot in this game; you orbit your way out of mistakes."

The middle segment of the page must feature a highly modular, data-driven "Simulation Log" grid that acts as the game's features and mechanics overview. Instead of boring marketing paragraphs or listicles, this panel breaks down the standard run progression structure from Waves 1 through 35, detailing boss anomalies like the Gravity Warden, Null Vector Seraph, and Magnetar Twins using stark, interactive wireframe silhouettes. Hovering over a boss profile triggers an audio-reactive glitch effect and displays their systemic mutation law (e.g., "PHYSICS MUTATION DETECTED: ARENA LAW: MIRROR WELL"). This section must communicate to roguelike build experimenters and speedrunners that the game is a deterministic, skill-based physics playground, using system readout language that frames encounters not as combat fights, but as spatial control and movement puzzles.

To leverage the game's core viral loop architecture (PLAY → CHAOS MOMENT → CLIP → SHARE → WISHLIST), design a dedicated, high-performance media slider titled "Watchable Impossibility Telemetry." This segment must natively stream ultra-short, high-frame-rate clips demonstrating true high-skill expression: gravity-assisted kills, frame-perfect recovery moments, and late-game Rupture escalation loops. Each clip container should display its associated run seed code overlayed on top, allowing users to hit a "Copy Seed" button to copy the string format `mode:seed:wave` straight to their clipboard. This architecture directly addresses secondary streaming audiences, presenting the website as an active terminal capturing real-time player reactions and community challenge runs rather than static marketing text.

As the visitor scrolls towards the bottom third of the layout, the website must undergo a calculated, scroll-driven visual mutation simulating the game’s late-game "Rupture" state. The clean, predictable grid geometry must intentionally distort; vector borders fracture across their axes, font weights shift randomly into corrupted character streams, and the background shifts into overlapping physics states with broken color logic. This is achieved via a performant fragment shader or CSS-driven transform layer that simulates a total wave generator failure, throwing warnings like `SYSTEM STABILITY CRITICAL` and `MUSIC DEFINES REALITY` across the layout. This transition must feel deeply impactful but strictly optimized, ensuring that layout adjustments do not drop frames or disrupt the user’s scrolling performance, serving as an automatic visual metaphor for the intense escalation players experience in-game.

Beneath the Rupture barrier, provide an expandable, keyboard-navigable interactive terminal designated as the "Anomalous Systems Codex." This interface breaks down the primary mechanical pillars of the game—Vectorfall, Arena Laws, Resonance Fields, Temporal Scars, and Gravity Scars—allowing optimization-focused players to read precise behavioral tooltips. Clicking a system term displays a clean vector diagram showing how overlapping gravity lines generate tactical field rules or how a local time wound alters projectile vectors. The writing style here must strictly conform to the developer-facing documentation: analytical, diagnostic, entirely objective, and absolutely devoid of generic sci-fi space fantasy lore, reinforcing the premise that survival is solely a product of analytical mastery.

The persistent footer and navigation layer must be framed as an active control panel anchored to the viewport boundaries to maximize conversion metrics. The header menu should be a minimalist, space-conscious bar tracking simulation metrics (e.g., `WISH_COUNT: ACTIVE`, `VERSION: 4.4_STABLE`) alongside instantaneous anchors to the Trailer, Sandbox, Codex, and Dev Progress logs. The footer must feature a massive, high-contrast terminal prompt block inviting the user to initialize a sequence by typing or clicking a single command: `EXECUTE_WISHLIST_REDIRECT`. This final call-to-action is flanked by direct social links to the high-priority platforms (TikTok for discovery, YouTube Shorts for replay loops, and Discord for seed challenges), driving the consumer directly into the game's self-sustaining community cycle.

When converting this blueprint into code using Cursor, Windsurf, or an AI agent, performance constraints must mirror the game's `PerformanceBudgetDirector` logic to prevent resource exhaustion and overdraw. All vector line animations must utilize native SVG operations, CSS hardware-accelerated transforms, or pre-allocated object pools within the HTML5 Canvas to guarantee a locked 60 FPS profile even on mobile browsers. Avoid loading heavy external UI packages, massive unoptimized tracking scripts, or raw, bloated video payloads; use deferred loading for all background clips, clamp maximum particle burst limits on the interactive canvas, and ensure that every visual pulse respects a global alpha restriction equivalent to the game's `Settings.flash_alpha()` safety cap to protect users from sudden fullscreen strobing.

This comprehensive blueprint serves as a definitive system prompt for automated workspace generation within Godot-adjacent web tooling or standard front-end frameworks like Svelte or Tailwind/TypeScript. When executing this plan, your codebase generation brain must treat this document as a strict specification matrix: variables must be strongly typed, assets must map to the explicit files documented in the development assets list, and typography choices must enforce simulation telemetry aesthetics. Reject any generated design component that introduces generic space capsule vectors, soft neon clouds, or power fantasy visual noise. The target deliverable must stand as a survivable visual paradox generator on the web—a digital asset that looks entirely unfakeable in motion, cleanly bridging the gap between immediate cognitive confusion and absolute structural mastery.

The current website is good, however, it must be better.